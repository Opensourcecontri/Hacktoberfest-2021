pull_request_rules:
  - name: automatic merge for master
    conditions:
      - "#approved-reviews-by>=1"
    actions:
      label:
        add: ['hacktoberfest-accepted']
      merge:
        method: merge
